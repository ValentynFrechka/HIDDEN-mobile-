import { View, Text } from "react-native";
import dimensionStyles from "../styles/skinsView.styles";

const SkinsView = () => {
    return (
        <View style={dimensionStyles.container}>
            <Text>Skins here</Text>
        </View>
    )
};

export default SkinsView;