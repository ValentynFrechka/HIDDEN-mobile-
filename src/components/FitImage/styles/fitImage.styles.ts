import { Dimensions, StyleSheet } from "react-native";

const fitImageStyles = StyleSheet.create({
    dimensions: {
        width: "100%",
        height: "100%"
    },
});

export default fitImageStyles;